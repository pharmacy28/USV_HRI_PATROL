# 人机协同 USV 区域覆盖搜索：研究备忘录

> 目的：这是一个持续维护的研究日志，用于沉淀每轮讨论中的想法、问题、决定与实验设计。最后更新：2026-08-03。

## 0. 项目一句话定义

在已知部分障碍、目标位置未知的二维矩形海域中，面向海上搜救，构建一个利用**人类意图、注意力与显式指令**来实时约束/修正多 USV 概率覆盖搜索策略的人机协同系统；目标是在同等安全约束和资源预算下，更快发现目标或更可信地排除目标存在。

关键不在于“人控制一艘船”，而在于：系统能将人的多模态行为转成有不确定度、可解释、可拒绝的算法输入，并证明这能带来可量化的边际收益。

## 1. 本轮用户原始构想（忠实记录）

- 已有无人船二维矩形海域区域覆盖搜索算法；多模态输入的人机协同路径规划算法仍在建设。
- 人机协同动机：计算机擅长底层高频重复计算，人擅长抽象判断；需明确协作方式、沟通方式、人的操作数据形态及其融入算法的途径。
- 已建设多模态人机协同仿真平台，基于 VRX humble 分支二次开发，捕捉：
  - 手柄：切换/控制 USV、切换 USV 视频流；
  - Tobii Pro Spark 眼动：屏幕实时注视位置；Unity 跨 Windows/Linux 数据传输；论文需写工作原理、跨平台标定及延迟处理；
  - 语音：本地部署大模型识别“某 USV 到某分区几何中心”，海域预先分区，已勘探障碍可栅格化用于避障与规划；需说明模型本体。
- 底层控制沿用 VRX WAM-V，调整动力参数以提高推力、加快搜索。
- 现行算法草案：
  1. 构建传感器距离—探测强度模型；
  2. 以贝叶斯后验更新二维栅格的目标存在信念和漏检率；
  3. 障碍物处理后的栅格图 K-means 聚类，匈牙利算法分配 USV—聚类中心，A* + 膨胀半径避障，定时更新聚类；
  4. 搜救情境：发现目标即停止；或全区漏检率低于阈值，判定区域内目标不存在并停止；
  5. 需建立基线；
  6. 计划三组：A 10 艘全自主；B 9 艘全自主 + 1 艘人工手动（人工数据不进算法）；C 9 艘全自主 + 1 艘人工，人工控制及语音剥离的目标信息进入算法。现设想为：将命令目标点与人控 USV 预测目标点强制作为聚类中心，再对其余部分聚类；
  7. K-means 初始值敏感，需合理设置；
  8. 寻求优于聚类分析的指派方法；
  9. 当前仅预测未来目标点，未预测未来轨迹；
  10. 人控 USV 目标点预测暂用随机森林、先前实验历史采样数据做分类；
  11. 担心预测点/轨迹实时性不足；
  12. 实验和指标尚待设计。

## 2. 论文与仿真平台：边界

### 2.1 论文主线（研究问题与可证伪贡献）

论文不是平台功能清单，而应回答：**何种人类信息、以何种置信度和机制进入概率搜索决策，能在何种条件下提升搜索效能，代价是什么？**

建议将贡献收敛为三层：

1. **多模态意图接口**：将语音命令、手柄操控、眼动注意力统一转成“人类建议（human cue）”及其时空不确定度，而不是把原始信号直接输入规划器。
2. **置信度门控的协同搜索决策**：用人类建议改变任务价值、约束或先验；当建议过期、置信度低或会造成安全/冗余时，算法降权或拒绝。该层是最可能形成方法创新的核心。
3. **可复现实验协议**：区分人实际贡献、接口贡献和算法贡献，量化效率、安全、覆盖质量、认知负荷及错误鲁棒性。

### 2.2 仿真平台主线（实验基础设施）

平台负责：VRX/Gazebo 物理与传感器仿真、USV 控制与视频流、Unity 操作界面、眼动与手柄采集、语音识别/命令解析、时间同步、日志、回放和实验条件控制。

平台可作为系统实现章节与开源/工程贡献，但不宜把“跨平台通信已实现”单独作为算法创新。它的学术价值在于：能以统一时间戳可靠地生成多模态协同实验数据，并支持消融。

### 2.3 当前分工建议

| 环境 | 优先工作 | 主要产物 |
| --- | --- | --- |
| Windows | 论文、实验协议、数据分析、图表、眼动仪厂商软件/标定与记录核验 | 论文草稿、实验表单、分析脚本/结果 |
| Linux（VS Code Remote） | VRX/Gazebo/ROS2 与 Unity 通信、算法节点、语音本地服务、日志回放、场景与测试 | 可复现仿真工作区、launch 配置、ROS2 节点、数据集 |
| 跨平台契约 | 仅定义消息、时钟、坐标系、延迟与日志格式 | 接口规范与测试记录 |

跨平台接口必须明确：`frame_id`、单调时间戳、坐标系/分辨率、序列号、测得延迟、置信度、数据版本。不要让 Windows 或 Linux 的本地时钟直接充当唯一事实来源。

## 3. 核心概念重构：从“强制聚类中心”到“人类建议的概率融合”

### 3.1 应区分的三类人类信息

| 信息源 | 可推断的语义 | 不应直接声称的语义 |
| --- | --- | --- |
| 语音命令 | 明确、离散、可追责的任务偏好/重规划请求 | 目标真实存在 |
| 手柄/航向/速度 | 操作员短时运动意图、风险回避或接近行为 | 长时全局战略 |
| 眼动落点/停留 | 关注区域、界面信息需求、可能的疑点 | 可靠的目标标注或意图本身 |

眼动应作为弱证据或置信度调节器，不能把“看了某处”直接当作“那里有目标”。语音为强显式意图，手柄为中等强度行为意图，眼动为弱隐式注意力；三者需要不同权重与失效处理。

### 3.2 统一中间表示（建议）

每次人机输入输出为：

`cue = {类型, 空间分布/轨迹分布, 时间窗, 置信度, 来源, 延迟, 有效期, 约束级别}`。

例：语音“USV3 到 4 区”生成一个区域任务建议；手柄行为生成未来 5–15 秒的人控艇占用走廊/意图分布；眼动落点经屏幕—世界坐标映射生成一个随时间衰减的关注热图。规划器接收的是这个中间表示，而非设备原始数据。

### 3.3 推荐的协同融合位置

不建议把人的目标点“硬编码为 K-means 初始/固定中心”。该做法可能造成不连续、不可行、重复覆盖，且很难论证眼动/手柄为何值得信任。

更稳妥的做法是把人类建议融入**任务效用图**或**信息增益图**：

`U(x,t) = 信息价值 + 人类建议价值 − 航行代价 − 碰撞/通信风险 − 与人控艇/其他艇的冗余代价`。

人类建议价值取决于来源可靠性、时效、与当前信念图的一致性及操作负荷。人控艇轨迹本身则作为时变占用/承诺区域进入约束，避免自主艇与其抢同一块区域。

明确命令可设置为软约束；若任务场景要求人工最终裁决，才设置为硬约束，并记录“强制执行”的原因。这样可做“仅硬约束”“置信度软约束”“无融合”的消融对比。

## 4. 传感器、概率地图与停止准则（待数学化）

### 4.1 传感器距离—探测模型的可行起点

将探测能力建成条件探测概率而不是模糊的“强度”：

`P_D(d, Δθ, z | c)`：在栅格 c 内存在目标时，距离 d、相对方位 Δθ、环境/海况 z 下本次观测发现目标的概率；
`P_FA(d, Δθ, z)`：无目标时误报概率。

第一版不必追求复杂物理建模，可使用截断逻辑函数/分段模型并明确参数来源：

`P_D(d) = P_D,max / (1 + exp[a(d - d50)])`，在最大量程外置零；再以视场、遮挡、可见性和扫掠时间乘性修正。

参数优先级：真实传感器标定数据 > 仿真中受控蒙特卡洛标定 > 合理假设并做灵敏度分析。论文必须声明它是仿真传感器模型，不能把假设参数包装成实测性能。

### 4.2 栅格信念与漏检率

每格维护目标存在概率/对数赔率，以及累计未探测概率。观测后按贝叶斯规则更新，应区分“未探测”和“阴性观测”。多艇、多时刻观测在条件独立是假设下可累积；若观测高度相关，应降权或合并，以避免虚假的过度自信。

建议把全局停止条件写成风险保证而非“所有格各自低于阈值”：例如区域仍存在目标的后验概率、或未发现目标的条件下全域漏检风险小于阈值，并同时给出最大任务时间/燃料/通信失效的安全停止条件。

## 5. 任务分解、分配与规划：方法选型

### 5.1 现有 K-means + 匈牙利 + A* 的定位

它适合作为透明、可实现的**基线/初版规划器**，但有三处结构性不匹配：

- K-means 的欧氏距离忽略障碍、航道与航行时间；
- 聚类中心不保证位于可航栅格或高信息价值位置；
- 匈牙利一次分配静态目标，不自然处理时变信念、人控艇占用与未来轨迹。

### 5.2 若保留 K-means，应这样处理

- 在自由空间连通域中聚类，距离采用 A*/代价地图近似的航行时间距离而非欧氏距离；
- 用 K-means++ 初始化，并以“上一轮质心 + 高价值未覆盖点 + 人类明确命令”作为 warm start；多次重启选最小加权惯性解；
- 聚类权重取信念/信息增益、漏检风险、未覆盖时长，而不是均匀栅格；
- 对人类命令设定锚点/软惩罚，不把所有预测点硬塞成中心；质心投影到可行自由空间；
- 以事件触发（新语音、显著后验变化、艇偏航/故障）加固定周期的混合机制更新，避免无意义抖动。

这可以解决初值敏感性，却不能根本解决“聚类是否等同于任务”的问题。

### 5.3 更值得比较的替代框架

1. **加权 Voronoi/市场竞价分区 + 局部信息路径规划**：每艇按到达时间/能耗划分责任区，适合在线、可解释、异构艇。
2. **CBBA/分布式拍卖**：任务为候选信息区域，价值中显式纳入人类建议和去重，适合多艇通信与动态重分配。
3. **滚动时域的信息驱动任务分配（推荐作为论文主方法候选）**：生成若干可行短航段/候选轨迹，以预期信息增益、航行代价、覆盖重叠和人类偏好评分，再用匈牙利/整数规划选一组非冲突动作；每 Δt 重规划。它天然支持从“目标点”提升到“轨迹”。
4. **POMDP/MPC**：理论最完整，但状态维数和多艇协同使工程与论文风险很高；不建议作为当前主线，除非范围缩小到单艇或小规模。

建议路线：K-means+匈牙利+A* 作为基线；“滚动时域、置信度门控的人类建议信息增益分配”作为拟创新方法。这样比较公平，且不必放弃已有实现。

## 6. 人控意图预测：从随机森林目标分类到可实时轨迹分布

随机森林可以保留为基线，但它只从历史数据预测离散终点，难以表达多模态意图、瞬时操控和不确定性。

渐进式方案：

1. **零预测基线**：只将人控艇当前/短时恒速度外推为动态障碍与已承诺覆盖区域。
2. **轻量意图模型（推荐首先落地）**：根据当前位姿、速度、航向、距离候选区域、最近语音、注视热图和历史窗口，输出候选区域/航向原语的概率分布；使用滑动窗口、10–20 Hz 推理、指数平滑和过期失效。
3. **轨迹层**：为前 k 个意图生成动力学可行轨迹（纯跟踪/运动基元即可），形成概率占用走廊；无需一开始训练大型轨迹网络。
4. **高级模型（后续）**：MDN、Transformer 或条件扩散可表达多模态未来，但应在数据规模、延迟和可解释性允许时再引入。

关键是：自主规划器不应把预测当真值，而应按其校准置信度把预测轨迹当作软风险/冗余代价。需记录预测提前量、ADE/FDE、意图 top-k 准确率、推理与端到端延迟。

## 7. 三组实验重述与增强

用户提出的 A/B/C 是必要的主对照，但 B 中人工艇可能天然改变覆盖策略，需控制其任务权限与总资源，避免把“多一名人”误判为“算法协同收益”。

建议最低实验矩阵：

| 组别 | 人工操控 | 多模态信息进入算法 | 目的 |
| --- | --- | --- | --- |
| A 自主 | 否 | 否 | 纯算法基线 |
| B 人在环未融合 | 是 | 否 | 分离人本身的直接作用 |
| C-硬融合 | 是 | 命令/预测点强制约束 | 检验当前设计的上限与脆弱性 |
| D-置信度协同 | 是 | 语音、手柄、眼动经门控软融合 | 主方法，与 C 区分 |

若人力允许，再做模态消融：语音单独、手柄单独、眼动单独、语音+手柄、全模态。每个场景应使用同一目标/障碍/传感器随机种子，采用交叉平衡的操作员顺序，防止熟练效应。

## 8. 指标与实验场景（草案）

### 8.1 任务效果

- 发现率/成功率，首次发现时间（TTD），任务完成时间；
- 停止时真实漏检率、停止时预测漏检风险、错误停止率；
- 后验地图质量：与真值相比的 Brier score、负对数似然、校准误差；
- 单位时间/单位航程的信息增益，覆盖率与有效覆盖率（扣除重复、不可观测与无效覆盖）。

### 8.2 协同质量与安全

- 人类建议采纳率、拒绝率与拒绝原因；建议到实际重规划的延迟；
- 人机/艇间重复覆盖率、最小艇间距离、碰撞/近失事件、越障或不可行规划次数；
- 航程、能耗代理、通信带宽与丢包下的性能退化。

### 8.3 人因与模型质量

- NASA-TLX、SART 或简短主观可用性量表；操作错误率、接管次数；
- 眼动：有效采样率、标定误差、注视识别延迟（作为接口质量，不直接当算法效果）；
- 意图预测：top-k 意图准确率、ADE/FDE、置信度校准、端到端 P50/P95 延迟。

### 8.4 最小场景集

- 开阔、稀疏障碍、狭窄航道、遮挡/死角；
- 目标先验均匀/有热点；传感器强/弱、误报/漏报不同；
- 正常人类提示、滞后提示、错误提示、冲突提示；
- 通信延迟/丢包和眼动/语音暂失效。

报告均值之外应报告置信区间、配对统计检验与效应量；不同操作员需要分层/混合效应分析，而不是将所有 trial 当独立样本。

## 9. 眼动、语音与跨平台实现中必须写清的工程事实

### 9.1 眼动

- 工作原理：近红外照明与眼部图像估计瞳孔/角膜反射，结合标定模型求屏幕凝视点；具体能力、采样率和标定要求必须以 Tobii Pro Spark 官方资料及实际版本为准。
- 坐标链：眼动设备/屏幕像素 → Unity UI/视频画面 → 相机射线/地图坐标 → ROS 世界坐标。每一步记录分辨率、裁剪、缩放、相机参数和 frame。
- 标定：每位被试、每次会话前标定并记录有效性；重启/座位变化/误差阈值触发重标定。请区分厂商眼动标定与应用层屏幕—世界映射标定。
- 延迟：对每条数据记录设备时间、接收单调时间、发布 ROS 时间和序号；采用固定延迟补偿 + 抖动缓冲/过期丢弃。测量而非猜测端到端延迟，并报告 P50/P95。

### 9.2 语音

语音链应拆为 ASR（音频到文本）、NLU/本地大模型（文本到结构化命令）、命令校验（艇号、区域、冲突与确认）。论文中说明所用本地模型、量化/硬件、提示词或语法约束、离线/在线模式、数据是否上传、命令准确率和延迟。高风险命令应有明确回显/确认机制。

## 10. 潜在创新边界与避免过度主张

可以主张的候选创新：多模态人类线索的统一不确定性表示；置信度、时效和一致性门控下的概率搜索任务重分配；以及在可控仿真中证明它提升效率且对错误人类提示鲁棒。

不宜单独主张为创新：A*、匈牙利算法、K-means、随机森林、贝叶斯栅格、VRX/WAM-V 参数调节、接入眼动或语音设备本身。它们可构成系统，但文献中均已有广泛基础。

应主动检索并对比的相邻研究：human-swarm interaction、shared autonomy、multi-robot active search / Bayesian search、intent-aware task allocation、gaze-based human-robot interaction、multi-modal operator intent inference。最终创新必须相对于这些工作用“输入—融合机制—决策对象—理论/实验保证—场景”逐项界定。

## 11. 暂不数学化、但必须在平台落地时采集的量

在平台未稳定前，不强行写最终问题公式。先统一日志，以支持后续建模：地图/障碍/真值、每艇状态与控制、传感器观测及模型参数、信念图快照、候选任务与得分、分配结果、路径、各类人类 cue、原始和校正时间戳、命令解析过程、设备有效性、随机种子、场景/软件版本。

## 12. 下一轮优先讨论的问题

1. 明确传感器种类与 VRX 中可获得的原始观测，确定第一版 `P_D/P_FA` 的可标定形式。
2. 固定“人类 cue → 任务效用图/约束”的接口和消息格式。
3. 选择主方法：保留 K-means 增强版，还是以滚动时域信息增益分配作为论文方法。
4. 列出现有 Linux 工作区的节点、通信链路和日志能力，形成平台开发清单。
5. 建立文献对比表，验证创新边界。

## 13. 本轮初步结论

- “人工数据接入算法”应定义为带可靠性、延迟和有效期的人类建议融合，而不应等同于硬塞目标点。
- 把预测从单点提升为短时概率轨迹/占用走廊，是更务实且可实时验证的路线。
- K-means+匈牙利+A* 保留为基线很合适；它不宜承载全部创新。
- 论文主张应围绕置信度门控的共享自主搜索，而平台专注于可靠的多模态数据闭环与实验可复现性。

## 14. 文献边界检查（第一轮，待系统综述）

- 已有共享自主研究使用递归贝叶斯过滤融合多种非语言观测来推断人的目标，故“从手柄/眼动推断意图”本身不能作为新意；本项目需要强调多艇概率搜索、线索时效/错误门控和对搜索风险的影响。
- 已有多机器人共享自主研究处理一位操作员对多机器人实时修正与调度，故“人同时参与多机器人”也不足够；区别应落在海上主动搜索的后验地图、信息价值任务分配及多模态输入接口。
- 人群机器人交互的研究已经将注意力分配视为关键人因变量；因此眼动的合理定位是估计注意力/输入可靠度，必须实证它优于只用语音和操控，而不能预设它等价于目标证据。
- 检索到的近期动态多机器人任务分配工作也已使用机器人意图向量；本项目“意图”必须明确是**人类多模态意图**且具有不确定性、延迟和撤销机制，避免概念重叠。

待阅读来源：Jain & Argall, *Probabilistic Human Intent Recognition for Shared Autonomy in Assistive Robotics* (ACM THRI, 2019)；Hagenow et al., *Coordinated Multi-Robot Shared Autonomy Based on Scheduling and Demonstrations* (2023)；Kolling et al., *Human Interaction with Robot Swarms: A Survey* (2016)；Chien et al., *Attention allocation for human multi-robot control* (2018)；以及近期 IAOM 动态多机器人任务分配工作。下一步建立逐篇“问题—输入—融合点—输出—实验—缺口”的对比表，不能仅凭摘要断言创新。

## 15. 第二轮修正：保留 K-means，分层处理硬命令与软线索

用户指出：现有 K-means 的栅格权重取目标存在信念，USV—目标点的代价使用避障后的路径长度；因此不能简单地批评为均匀栅格上的欧氏聚类。此修正成立。需在实现中明确：加权 K-means 的质心更新仍是几何意义的；若聚类阶段也要严格使用障碍后最短路径距离，可改为加权 K-medoids，或保持 K-means 生成候选区域、再用 A* 路径代价做分配。两者都可作为可说明的设计选择。

### 15.1 最简协同规则

不要把所有人类输入软化。将它们分成两层：

1. **明确语音命令**（例如“USV 3 前往 4 区”）是对指定艇的硬任务：通过语法/艇号/区域校验且目标可行时，立即回显确认，将该艇从本轮聚类与匈牙利分配中移除，令其航向区域几何中心在自由空间中的最近可行投影；A* 仅负责安全到达。若不可行，必须解释拒绝原因并要求确认替代点。这是信任的前提，而不是软约束。
2. **操控预测与眼动**不是命令，只作为其余艇的软去重信息：将人控艇未来短时可覆盖的栅格以概率 `h(c)∈[0,1]` 表示，供余下自主艇使用的聚类权重为 `w_res(c)=w(c)[1-h(c)]`。其中 `w(c)` 是目标存在信念/搜索价值。最朴素的第一版可令 `h(c)` 为人控艇恒速度外推路径周围的固定宽度走廊；眼动只在该走廊或相应分区上给 `h(c)` 一个小的增量，且随时间指数衰减。

这一规则只包含“命令艇保留 + 其余艇避开预期覆盖”两件事，避免设计庞大效用函数；同时确保显式命令得到执行。

### 15.2 直接可实现的轮次算法

每次重规划：解析有效命令集合 A；固定每个 `i∈A` 的可行目标；用人控艇短时预测产生 `h(c)`；对未被固定的艇，在 `w_res(c)` 上运行加权 K-means（或 K-medoids），簇数为 `N-|A|`；以 A* 路径长度构造代价矩阵并用匈牙利算法分配；固定艇和自主艇均由 A* 执行。新命令/障碍变化/固定周期触发下一次更新。

## 16. 摄像机视频流：概率探测、地图更新和内容预警（第一版模型）

### 16.1 观测和标定

对每帧或经空间去相关抽样后的图像，检测器在图像区域/投影栅格 c 给出目标类别置信分数 `s`。先在标注视频中按距离 d、相对方位 θ、海况/光照 z 分桶，选择阈值 τ，经验估计：

`P_D(d,θ,z;τ)=P(s≥τ | 目标存在)`，
`P_FA(d,θ,z;τ)=P(s≥τ | 目标不存在)`。

这两个量比直接拿检测器的置信度当概率更可靠。可进一步用 temperature scaling 或 isotonic regression 将 `s` 校准为概率。标定集与测试集必须按场景/视频序列分开，防止相邻帧泄漏导致虚高结果。

### 16.2 一次阴性或阳性观测的贝叶斯更新

设栅格 c 的先验存在概率为 `p^-`，当前帧在该格给出二元告警 `z∈{0,1}`。阳性时：

`p^+ = p^- P_D / [p^- P_D + (1-p^-)P_FA]`。

阴性时：

`p^+ = p^-(1-P_D) / [p^-(1-P_D)+(1-p^-)(1-P_FA)]`。

对一次连续视频片段中的连续阴性，不能把每一帧当作独立证据。先按相机覆盖足迹移动距离、视角变化或图像相似度选取近似独立的 K 个视图，再使用 `P_miss=∏_{k=1}^{K}(1-P_D,k)`；第一版可取 `K_eff=min(K, 总航程/有效足迹直径)`。若连续 K 个独立视图的 `P_D` 相同，累计探测概率为 `1-(1-P_D)^K`。

### 16.3 从图片内容到预警

建议先做三类可解释输出：目标类检测（人员/救生艇/漂浮物/飞机残骸，视你的任务标签而定）、危险/障碍物检测、未知异常。每个候选检测必须投影为地图中的区域而非仅屏幕框。

对同一空间目标轨迹的多帧检测，使用跟踪器/卡尔曼滤波把分数融合；只有同时满足“连续或独立视图确认数 ≥ m”“融合后后验 ≥ 阈值”“位置可投影且未被规则排除”才发红色确认预警。低一级的黄色预警只生成候选区域和请求人工复核，不改变全局停止结论。这样，误报不会直接终止/劫持搜索。

### 16.4 SNR、真阳/假阳与 P_D/P_FA

最干净的理论起点是二元检测：观测统计量 y 在无目标时 `H0: y~N(0,σ²)`，有目标时 `H1: y~N(μ,σ²)`，定义 `SNR=μ²/σ²`。以阈值 γ 报警：

`P_FA=Q(γ/σ)`，
`P_D=Q((γ-μ)/σ)=Q(Q^{-1}(P_FA)-√SNR)`。

因此在固定误报率下，提高 SNR 会提高真阳率；提高阈值会降低假阳，也会降低真阳。这是 ROC 曲线的来源。摄像机深度检测器的置信分数并不必然满足高斯假设，故在论文实施中应以实测 ROC/PR 曲线为主；可把 `d'=Φ^{-1}(P_D)-Φ^{-1}(P_FA)` 作为“等效可分离度”，而非宣称原始图像 SNR。海面反光、雾、浪花、目标像素尺寸和压缩失真会降低有效 SNR，最终表现为同一阈值下 P_D 降低或 P_FA 升高。

## 17. 云端同步（GitHub）

本机已安装 Git，但未检测到 GitHub CLI (`gh`)；可无需安装 CLI，直接以浏览器创建私有仓库后用 Git 推送。上传前严禁提交令牌、密码、设备密钥、录屏中可能含个人生物信息的数据、或大体积原始视频/眼动数据。原始实验数据建议另行做权限与存储设计。

推荐仓库定位为私有研究记录库，例如 `human-usv-search-notes`。通过 GitHub 网页新建空的 **Private** 仓库，不要勾选 README、`.gitignore` 或 License；然后取得 HTTPS 远程地址。得到地址后，可在项目根目录依次执行 Git 初始化、提交备忘录、添加远端与推送。若使用 HTTPS，GitHub 认证使用浏览器登录或个人访问令牌，不能使用账户密码。
